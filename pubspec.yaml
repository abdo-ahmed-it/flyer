name: flyer
description: "A CLI tool to speed up Flutter development by automating feature creation, project initialization, deep linking, localization, and code generation."
version: 1.4.0
homepage: https://github.com/abdo-ahmed-it/flyer

environment:
  sdk: ^3.5.4

executables:
  flyer: flyer

dependencies:
  yaml: ^3.1.3
  dart_style: ^3.1.2
  args: ^2.7.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
